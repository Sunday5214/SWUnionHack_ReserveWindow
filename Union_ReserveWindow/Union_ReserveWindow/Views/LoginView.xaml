<Page
    x:Class="Union_ReserveWindow.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Union_ReserveWindow.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid Background="#FF6F61" Opacity="0.8" Visibility="{Binding ThisVisible}">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbStatus" Grid.Row="0" Text="{Binding StatusText}" VerticalAlignment="Bottom"
                   Foreground="White"
                   HorizontalAlignment="Center" FontSize="60"/>
        <TextBox Grid.Row="1" Width="600" Height="70" VerticalAlignment="Top"
                 Margin="0 10 0 0" KeyDown="TextBox_KeyDown"
                 FontSize="40" x:Name="tblogin"/>
    </Grid>
</Page>
